config:
  target: "ws://127.0.0.1:8081/"
  phases:
    - duration: 60
      arrivalRate: 100
      rampTo: 1000
      name: "Fire in the hole!"
scenarios:
  - engine: "ws"
    flow:
      - send:
          type: 'boleh'
